# Northstar admitted-profile handoff

`m2_product_profiles.json` records the two valid product specification fixtures,
their source hashes, and the hand-authored expected predicate shapes consumed by
future M3 tests. It is not an admission result or a runtime QC plan.
