# Synthetic fault injection

`hero.py` derives the M2 hero fixture in a new destination without modifying
the clean original. Its four faults are declared in the generated `faults.json`
ledger so intentional defects are distinguishable from generator failures.
