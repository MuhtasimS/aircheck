# Synthetic generator

`last_lightkeeper.py` generates the one-second 1080p/23.976 The Last
Lightkeeper clean package and invokes the bounded M2 fault injector. It writes
only into a caller-owned empty destination.
