# Synthetic source

`m2_expected_hashes.json` freezes SHA-256 facts for the deterministic
one-second clean and hero packages generated by M2. It records the verified
FFmpeg environment because media-container bytes are tool-version-sensitive.
